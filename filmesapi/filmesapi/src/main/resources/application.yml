spring:
  application:
    name: Filmes API
  datasource:
    url: jdbc:h2:mem:filmes
    username: sa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console
